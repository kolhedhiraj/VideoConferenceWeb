{% extends "VideoConferenceWebsiteBundle::layout.html.twig" %} 
{% block content %}
<div>


	{% for type, messages in app.session.flashBag.all %} {% for message in
	messages %}
	<div class="{{ type }}" style="text-align: center;">
	<strong>{{ message|trans({}, 'FOSUserBundle') }}</strong></div>
	{% endfor %} {% endfor %}

	<div>{% block fos_user_content %} {% endblock fos_user_content %}</div>

</div>
{% endblock %}
